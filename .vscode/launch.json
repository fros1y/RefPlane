{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "iOS: Run on Simulator",
      "type": "sweetpad-lldb",
      "request": "launch",
      "preLaunchTask": "sweetpad: launch",
      "appPath": "${command:sweetpad.build.resolveAppPath}"
    }
  ]
}
